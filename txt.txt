Super Admin has all access no school id ,
admin which has its own table have access of everything except roles, permissions, schools and
no one can see super-admin.

Error:
    1. Admin is not l0gin in /admin.
    2. While creating user no school_id is been auto loaded.

Suspicous:
    1. Adding Auth::user() rather than auth()->user().
